<div class="container ">
  <%= form_with url: users_path, model:@users, local:true do |f| %>
  <%= f.label :username, "Username" %>
  <%= f.text_field :username %>
  <br>
  <%= f.submit 'Sign In', :class => 'btn btn-dark' %>
</div>
<% end %>
